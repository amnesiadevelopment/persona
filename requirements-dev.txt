pytest>=9.0
pytest-timeout>=2.4

# The parser every workflow-SHAPE guard reads its subject through (PS-389).
#
# DECLARED HERE BECAUSE IT WAS DECLARED NOWHERE. Until this line, PyYAML
# appeared in none of requirements.txt, requirements-dev.txt or pyproject.toml,
# and reached `ci.yml` / `release.yml` only as a TRANSITIVE dependency of
# `uvicorn[standard]` under `pip install .` — a route nothing pinned for this
# purpose and no test asserted. Four workflows that do not run `pip install .`
# had each learned `pip install ... PyYAML` by hand, every one of them after
# being bitten: `chromium-upstream-watch.yml`, `firefox-major-watch.yml`,
# `published-engine-verdict.yml` and `engine-continuity.yml`.
#
# ⚠️ THIS LINE DOES NOT REPLACE THOSE FOUR, AND MUST NOT BE READ AS DOING SO.
# They install requirements-dev.txt too, so this makes their explicit `PyYAML`
# argument redundant — but four tests assert that argument is present by name
# (test_ps342_chromium_watch.py, test_ps372_firefox_major_watch.py,
# test_ps370_published_engine_gate.py, test_ps375_engine_continuity_gate.py),
# and they are guarding the "never works" class where a clean `setup-python`
# meets a bare `python -m pytest`. Belt and braces is correct here: this entry
# is what makes the `yaml` capability in conftest.py HONOURABLE on every job,
# and those arguments are what keep it honourable if this file's install line
# is ever reordered behind the self-test.
#
# ⛔ A DECLARED DEPENDENCY IS NOT A POLICED ONE. If this line went missing, or
# an install of it failed, the 97 workflow-shape guards it feeds would go back
# to skipping SILENTLY — which is why the same commit that added this line
# named `yaml` in conftest.py's CAPABILITIES table. The declaration is what
# makes the absence loud; this line is what makes the declaration fair.
PyYAML>=6.0

# Type checker (PS-165). Declared here because it was previously installed by
# nothing and invoked by nowhere — mypy.ini had sat in the repo unused, and the
# ONLY reason it went unrun was this absence.
#
# BOUNDED DELIBERATELY, not merely floored. mypy's diagnostics are
# version-sensitive: new releases add checks and reword messages, so a bare
# `mypy>=2.3.1` would let a fresh minor change the checker's verdict with no
# change to this repo — a gate that answers differently on Tuesday is not a
# gate. The upper bound keeps the advisory run's error count reproducible;
# raise it deliberately, by running the check and re-measuring, not reflexively.
#
# MEASURED AT 2.3.1 (Python 3.12): the advisory baseline is whatever
# `python3 -m mypy --config-file mypy.ini src` reports on the current tree.
# The number is deliberately NOT frozen here — it moves when the code moves and
# when this bound moves, and a count pinned in a comment goes stale silently
# and is then quoted as current. Read it from the run.
#
# The narrow CONFORMANCE gate does not depend on this pin at all — it compares
# signatures with the stdlib `ast` module and would hold its verdict even if
# mypy were absent. See .github/scripts/check_protocol_conformance.py.
mypy>=2.3.1,<3
