pytest>=9.0
pytest-timeout>=2.4

# PyYAML (PS-389). Declared here because it was previously declared NOWHERE —
# not in requirements.txt, not in requirements-dev.txt, not in pyproject.toml —
# while 89 tests parse a workflow file with it, 37 of them the gate-shape
# guards in tests/test_ci_verification_gates.py that assert the OTHER
# capability declarations exist.
#
# It reached the tests job only as a TRANSITIVE dependency of
# `uvicorn[standard]` under `pip install .`. That route is real and works
# today; it is also an accident. Nothing pins it for this purpose, no test
# asserts it, and the day uvicorn drops the extra those 89 tests go quiet.
# Three workflows had already learned this by hand, each after being bitten —
# see the `pip install ... PyYAML` lines in chromium-upstream-watch.yml,
# firefox-major-watch.yml and published-engine-verdict.yml.
#
# ⛔ DECLARING IT HERE IS HALF THE FIX AND MUST NOT BE MISTAKEN FOR ALL OF IT.
# A declared dependency that fails to install still skips SILENTLY: the defect
# is re-created one level up, with a requirements file as its new hiding place.
# The other half is conftest.py's `yaml` capability, declared in ci.yml — that
# is what turns the absence into a failure instead of a green. The two land
# together or neither is worth anything.
#
# Unbounded deliberately, unlike mypy below: this is a YAML parser read by
# workflow-shape tests through `safe_load`, not a checker whose verdict is
# version-sensitive. A floor would pin nothing a test depends on.
PyYAML>=6.0

# Type checker (PS-165). Declared here because it was previously installed by
# nothing and invoked by nowhere — mypy.ini had sat in the repo unused, and the
# ONLY reason it went unrun was this absence.
#
# BOUNDED DELIBERATELY, not merely floored. mypy's diagnostics are
# version-sensitive: new releases add checks and reword messages, so a bare
# `mypy>=2.3.1` would let a fresh minor change the checker's verdict with no
# change to this repo — a gate that answers differently on Tuesday is not a
# gate. The upper bound keeps the advisory run's error count reproducible;
# raise it deliberately, by running the check and re-measuring, not reflexively.
#
# MEASURED AT 2.3.1 (Python 3.12): the advisory baseline is whatever
# `python3 -m mypy --config-file mypy.ini src` reports on the current tree.
# The number is deliberately NOT frozen here — it moves when the code moves and
# when this bound moves, and a count pinned in a comment goes stale silently
# and is then quoted as current. Read it from the run.
#
# The narrow CONFORMANCE gate does not depend on this pin at all — it compares
# signatures with the stdlib `ast` module and would hold its verdict even if
# mypy were absent. See .github/scripts/check_protocol_conformance.py.
mypy>=2.3.1,<3
